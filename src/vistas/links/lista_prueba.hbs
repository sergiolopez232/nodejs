<div class="container p-4">
    <div class="row">
        {{#each links}}
            {{this}}
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>There are not Links saved yet.</p>
                <a href="/links/nuevo">Create One!</a>
            </div>
        </div>
        {{/each}}
    </div>
</div>